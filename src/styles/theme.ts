'use client'
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#006cff',
    },
    secondary: {
      main: '#4b8b8d',
    },
  },
});

export default theme;